<!--配車履歴パーシャル-->
  <% if offers.any? %>
    <% offers.each do |offer| %>
      <span><%= offer.user.name %> 様から <%= offer.created_at %> にメッセージが送られてきました。</span>
      <p>配車希望場所 : <%= offer.offer_content %></p>
    <% end %>
    <%== pagy_bootstrap_nav(@pagy) %>
  <% end %>